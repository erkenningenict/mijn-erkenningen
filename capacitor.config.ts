import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'new.ionic.v7',
  appName: 'new-ionic-7',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
